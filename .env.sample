PORT=3002
WEB_APP_ORIGIN=http://localhost:8081
WEBSOCKET_AUTH_TOKEN=
OVERRIDE_START_SCORE=
# Enable restart game feature for development (set to 'true' to enable)
ENABLE_RESTART_GAME=false

# === ACTIVE SUPABASE ENVIRONMENT: LOCAL ===
#
# Use `pnpm run env:local` to activate local Supabase (fetches credentials automatically)
# Use `pnpm run env:remote` to activate remote Supabase
# Use `pnpm run env:status` to see current status

# --- LOCAL Supabase (from `npx supabase start`) ---
# (run `pnpm run env:local` to populate these automatically)
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=

# --- REMOTE Supabase (hosted) ---
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-remote-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-remote-service-role-key

# --- Other Supabase Settings ---
